apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  values:
    ingress:
      hosts:
        - host: media.men.sh
          paths:
            - path: /
              pathType: ImplementationSpecific
