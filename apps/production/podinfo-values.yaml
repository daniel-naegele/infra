apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  values:
    ingress:
      hosts:
        - host: podinfo.men.sh
          paths:
            - path: /
              pathType: ImplementationSpecific
